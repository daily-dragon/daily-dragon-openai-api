Create ${n} short, natural ${targetLanguage} sentences using these words: ${words}. Each sentence uses one target word once in a typical, everyday context. ${hskLevelInstruction}Design each sentence so the exact target word is the only natural translation — if the word has usage restrictions (e.g. 二 vs 两), choose a context that specifically requires it. Enclose the translated target word in < >.

Return JSON only (no extra text):
{
  "sentences": {
    "original_word1": "sentence with <translated_word>",
    ...
  }
}