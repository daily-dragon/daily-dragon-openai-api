Create ${n} short, natural sentences using these words: ${words}. ${hskLevelInstruction}For each word:
1. Compose a Chinese sentence that uses the target word correctly and naturally.
2. Translate that sentence into ${targetLanguage}, enclosing the translation of the target word in < >.

Each sentence must use the target word exactly once in a typical, everyday context.

Return JSON only (no extra text):
{
  "sentences": {
    "original_word1": "sentence with <translated_word>",
    ...
  }
}